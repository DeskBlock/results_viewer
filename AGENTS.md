# 開発ガイド

## 構成と実行

FEM Frame Viewer は、ローカルの連番画像を最大4枠で比較するブラウザアプリです。HTML・CSS・JavaScript はルートの `index.html` に集約しています。利用方法は `README.md` に記載します。

依存ライブラリ・ビルド設定・リポジトリ内の自動テスト基盤・サンプル画像はありません。`webkitdirectory` に対応したブラウザで `index.html` を直接開いて確認できます。必要ならルートで `python3 -m http.server 8000 --bind 127.0.0.1` を実行し、`http://127.0.0.1:8000/` にアクセスします。

## 現在の仕様

- ブラウザの表示領域全体を使用。1枠は全領域、2枠は左右、3〜4枠は2×2です。
- 各フォルダの `image/` MIMEタイプのファイルをファイル名の自然順に並べ、同じ配列位置を同じ Step として表示します。
- 内部インデックスとスライダーは0始まり、画面と保存ファイルの Step は1始まりです。画像なしの場合は0です。
- Step 総数は表示中の枠の最大画像枚数。範囲外の枠は画像なしのメッセージを表示します。
- ボタン、スライダー、Step 入力＋Enter、左右矢印、スペースキーで操作します。入力や境界線の操作と全体のショートカットを競合させないでください。
- Speed は1・2・5・10・20・30・60 FPS。Loop オフでは末尾停止、オンでは先頭に戻って継続します。
- フォルダ・表示数の変更時は停止して先頭へ戻ります。非表示の枠の画像列は保持します。
- 境界線のドラッグと矢印キーで左右・上下の比率を20〜80%に調整可能。境界線のダブルクリック／Homeと「均等に戻す」でリセットできます。
- 枠の見出しはフォルダ名・枚数・実際に表示中のファイル名。ファイル名の更新はデコード完了後の画像交換に合わせます。

## 実装上の役割と注意点

- `panels`：4枠の画像列、表示画像、Object URL、処理中の画像、枠ごとのキャッシュを管理。
- `showFrame()` / `renderPanels()`：Step を指定し、次の画像をデコードしてから要素を交換。待機中は現在の画像を維持します。
- `cacheImage()` / `prefetch()` / `trimCache()`：3 Step先と直前を先読み・再利用。各枠で最大6枚、推定64 MiBを目安に保持し、表示中・表示要求中は容量制限から除外。先読みは枠ごとに順次実行し、Loop時は全体のStep総数で折り返します。
- `releaseEntry()` / `clearImage()`：古い非同期処理を無効化し、不要な Object URL を解放。高速移動、読み込み失敗、フォルダ変更、非表示化、ページ離脱を考慮してください。
- `updateFileCount()`：表示画像に対応する見出しとツールチップを更新。
- `play()` / `stop()`：単一の再生タイマーを管理。FPS変更、Loop切り替え、画像0枚・1枚でもタイマーを重複・放置しないこと。表示待ちの `pending` がある間は次のStepに進めず、先読み処理自体は再生を止めないこと。
- `layout` / `setSplit()` / `updateLayout()`：分割比率、CSSのグリッド寸法、境界線位置、ARIA値を同期。境界線幅とグリッド間隔はともに8pxです。
- `serializeSession()` / `deserializeSession()` / `restoreSession()`：画像込みJSONの保存・検証・復元。全件の検証完了前に既存状態を変更しないでください。

## 保存形式と互換性

新規保存は `format: "fem-frame-viewer"`, `version: 1`。`viewCount`, `fps`, `step`, `loop`, `layout: { x, y }`, 全4枠の `panels` を保存します。各枠は `folder`, `images` を持ち、画像は `name`, `type`, Base64のData URLである `data` を持ちます。

旧ファイルで `loop` がなければfalse、`layout` がなければ各0.5に戻します。既存の `version: 2`, `mode: "embedded"` も読み込み対応を維持します。フォルダ参照のみのモードは削除済みです。復元後に自動再生はしません。

保存は処理開始時点の状態を取得し、非表示枠も含めます。画像を含むため大容量になり得ます。画像や保存JSONをリポジトリに追加しないでください。

## 変更方針

- 単一ファイル・依存ライブラリなしの構成を基本とし、無関係な整形や構成変更は避けてください。
- 周辺のスタイル、JavaScriptの `const` / `let`、ダブルクォート、セミコロンに合わせます。
- 日本語の案内と既存の英語ラベルを尊重し、機能変更に伴ってREADMEを更新してください。
- 画像やファイル名はローカルで処理し、外部送信を追加しないでください。利用者由来の文字列は `textContent` 等で表示し、HTMLに挿入しないこと。
- メモリ解放、古いデコード結果の無視、タイマーの停止、保存形式の互換性を維持してください。

## 確認項目

変更範囲に応じて確認してください。定常的な自動テストコマンドはまだありません。開発中の一時的なNode.jsのDOMモックテストでは、画像交換、保存復元、分割比率、Loop等の処理を確認していますが、実ブラウザでの描画検証の代わりにはなりません。

1. 初期状態：Step 0 / 0、スライダー・移動ボタン無効。画像なしでPlayしても動作しない。
2. 読み込み：画像と非画像の混在、自然順、1〜4枠、件数差、空フォルダへの変更、フォルダ変更時の停止とリセット。
3. 移動：各ボタン・スライダー・キーボード・Step入力。空欄・小数・範囲外の入力、Enter前のフォーカス離脱。
4. 画像交換：黒画面を挟まないこと、表示ファイル名の一致、高速操作で古い画像に戻らないこと、デコード失敗時の表示とURL解放。キャッシュヒット、保持枚数・推定容量制限、Loop先読み、古い先読みの無効化も確認。
5. 再生：各FPS、末尾停止、Loopの周回、再生中のLoop/FPS変更、画像1枚、手動停止。
6. 分割：2〜4枠でのドラッグ、20〜80%制限、矢印キー、Home、ダブルクリック、均等化、ウィンドウサイズ変更。
7. JSON：画像・順序・非表示枠・Step・FPS・Loop・分割比率の復元、旧形式の既定値、不正形式で現在状態を維持すること。
8. ブラウザ：16:10を含む表示領域での画像の縦横比、長いファイル名、コンソールエラーを確認。

完了時は変更内容、実施した確認、未確認事項を簡潔に報告してください。文書のみの変更では実装との整合性を確認します。
